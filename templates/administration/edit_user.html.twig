{% extends 'base.html.twig' %}

{% block title %}{{ isNew ? 'Créer' : 'Modifier' }} un utilisateur{% endblock %}

{% block body %}
    <h1>{{ isNew ? 'Créer' : 'Modifier' }} un utilisateur</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">{{ isNew ? 'Créer' : 'Modifier' }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_administration') }}">Retour à la liste</a>
{% endblock %}